spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ongodmatchu
    username: woong
    password:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
